-- Remove this when Ghostty gets its own smear-cursor
-- Kitty already has a smear cursor

-- Doesn't seem to work well at least not when tested
-- The cursor color is changing when moving around which is annoying
return {
	-- "sphamba/smear-cursor.nvim",
	-- opts = {},
}
